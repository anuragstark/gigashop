apiVersion: v1
kind: Service
metadata:
  name: gigashop-service
  namespace: gigashop
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30000
  selector:
    app: gigashop
