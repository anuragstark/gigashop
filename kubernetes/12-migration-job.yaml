apiVersion: batch/v1
kind: Job
metadata:
  name: db-migration
  namespace: gigashop
spec:
  template:
    spec:
      containers:
      - name: qbs-migration
        image: anuragstark/gigashop-migration:4
        imagePullPolicy: Always
        env:
        - name: MONGODB_URI
          value: "mongodb://mongodb-service:27017/easyshop"
      restartPolicy: OnFailure
